name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_test.txt

      - name: Lint (ruff)
        run: ruff check custom_components/familyboard_caldav tests

      - name: Format check (ruff)
        run: ruff format --check custom_components/familyboard_caldav tests

      - name: Run tests
        run: pytest
